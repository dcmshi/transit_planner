# GTFS Static feed URL (GO Transit / Metrolinx Open Data)
# Primary (current): Metrolinx CDN
GTFS_STATIC_URL=https://assets.metrolinx.com/raw/upload/v1683228856/Documents/Metrolinx/Open%20Data/GO-GTFS.zip
# Historic/fallback: https://www.gotransit.com/static_files/gotransit/assets/Files/GO_GTFS.zip

# GTFS-Realtime feed URLs (OpenMetrolinx Open Data API)
# Requires registration + API key at: https://api.openmetrolinx.com
# Append ?key=YOUR_API_KEY to each URL, or set GTFS_RT_API_KEY below
GTFS_RT_TRIP_UPDATES_URL=https://api.openmetrolinx.com/OpenDataAPI/TripUpdates
GTFS_RT_VEHICLE_POSITIONS_URL=https://api.openmetrolinx.com/OpenDataAPI/VehiclePositions
GTFS_RT_ALERTS_URL=https://api.openmetrolinx.com/OpenDataAPI/ServiceAlerts
GTFS_RT_API_KEY=

# How often to refresh static GTFS data (hours)
GTFS_REFRESH_HOURS=24

# How often to poll GTFS-RT feeds (seconds)
GTFS_RT_POLL_SECONDS=30

# Database (defaults to local SQLite; swap for PostgreSQL in production)
DATABASE_URL=sqlite:///data/transit.db

# Local Ollama server — required for ?explain=true
# Install: https://ollama.com  then: ollama pull llama3.2
# Recommended models:
#   llama3.2      — fast, ~2 GB VRAM/RAM, good for explanation tasks (default)
#   llama3.1:8b   — higher quality, ~4.5 GB RAM
#   mistral       — good balance of speed and quality, ~4 GB RAM
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# API server
API_HOST=0.0.0.0
API_PORT=8000

# Routing constraints
MAX_ROUTES=5
MAX_TRANSFERS=2
MIN_TRANSFER_MINUTES=10
